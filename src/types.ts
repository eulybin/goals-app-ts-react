export type CourseGoal = {
    title: string;
    description: string;
    id: number;
};

export type NewGoal = {
    goal: string;
    summary: string;
};

export type ImageObject = {
    src: string;
    alt: string;
};
